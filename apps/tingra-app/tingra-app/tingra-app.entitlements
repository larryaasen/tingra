<?xml version="1.0" encoding="UTF-8"?>
<!--
  tingra-app.entitlements
  tingra-app

  Created by Larry Aasen on 2026-08-01.
  Copyright © 2026 Larry Aasen.
  SPDX-License-Identifier: MIT

  Notarization requires the hardened runtime, which denies camera and
  microphone access unless the binary opts in — hence the two device
  entitlements. disable-library-validation lets the host load a camera's
  CMIOExtension DAL plug-in, and third-party plug-ins generally, signed by
  a different team than this app (without it, CMIOGraphStart fails and no
  camera ever streams; the same fix appears in
  apps/tingra-cli/tingra-cli.entitlements and
  apps/tingra-cameras/tingra-cameras/tingra-cameras.entitlements). No
  sandbox entitlement: Tingra is deliberately unsandboxed. Screen Recording
  needs no entitlement — it is TCC-only.

  keychain-access-groups puts this app and tingra-cli in one data-protection
  keychain group, so the daemon can read a stream key the operator filed here
  (DESTINATIONS.md, "Key sharing between the app and the daemon"). The value
  carries the $(TeamIdentifierPrefix) build variable, which Xcode expands at
  build time from DEVELOPMENT_TEAM — a literal Team ID must never reach a
  tracked file (CLAUDE.md, "Signing"). Two consequences worth knowing:
  scripts/sign-app.sh re-signs with --preserve-metadata=entitlements, so the
  expanded group survives, but only if that identity belongs to the same team
  the group is prefixed with; and a CODE_SIGNING_ALLOWED=NO build (how CI
  builds) carries no entitlements at all, which is the same honest degradation
  an unsigned CLI has.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>keychain-access-groups</key>
    <array>
        <string>$(TeamIdentifierPrefix)com.moonwink.tingra.shared</string>
    </array>
</dict>
</plist>
